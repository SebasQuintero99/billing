import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Sin configuración experimental por ahora
  // Turbopack se ejecutará por defecto en Next.js 16
};

export default nextConfig;
